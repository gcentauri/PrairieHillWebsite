

.ccf-main-img {
    //height: 400px;
}

#ccf-menu {
    @extend .bottom-shadow;
    position: fixed;
    //width: 100%;
    margin: 0 auto;
    bottom: 0;
    //background: rgba(255,255,255,0.5);
    //background: rgba(234,222,26,0.5);
    z-index: 10;
}

#ccf-join-msg {
    position: absolute;
    //top: 45%;
    right: 0;
    background: rgba(255,255,255,0.7);

    p {
	text-align: justify;
    }
}

#ccf-title {
    background-color: white;
    padding-bottom: 9px;
    border-radius: 2px;
}