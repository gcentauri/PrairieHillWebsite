@import url(http://fonts.googleapis.com/css?family=Roboto:400,100,100italic,700italic,700);
@import url(http://fonts.googleapis.com/css?family=News+Cycle:400,700);
@import url(http://fonts.googleapis.com/css?family=Clicker+Script|Kaushan+Script);
@import url(http://fonts.googleapis.com/css?family=BenchNine|Poiret+One|Open+Sans+Condensed:300|Playball|Shadows+Into+Light+Two);


$phill-grn: #3f8000;
$phill-gold: #ffef19;
$phill-blu: #e3f5fa;
$phill-blu-light: #ebf7fa;
$nav-pills-border-radius: 0em;
$nav-pills-active-link-hover-bg: $phill-grn;
$nav-link-hover-bg: $phill-gold;
$font-family-sans-serif: 'News Cycle', 'Roboto';
$link-color: $phill-grn;


@import 'bootstrap';

html, body {
    height: 100%;
}

h1 {
    font-size: 60px;
}

h2 {
    font-size: 52px;
}

p {
    text-align: justify;
    padding: 20px;
    font-size: 20px;
}

.section-title {
    font-family: 'Shadows Into Light Two', 'Playball', 'Open Sans Condensed', 'Poiret One', 'BenchNine', 'Kaushan Script', 'Clicker Script';

    color: white;
    background: $phill-grn;
    opacity: 0.9;

    position: relative;
    padding: 12px 0 0 12px;
    border-radius: 2px;
}

.section {
    padding: 20px 0 10px 0;
    clear: both;
}

.blue-box {
    margin-top: -30px;
    background: $phill-blu;
    padding: 25px;
}

.blue-box-light {
    @extend .blue-box;
    background: $phill-blu-light;
}

.important-link {
    font-size: 25px;
    font-weight: bold;
    padding: 10px;
    float: right;
    clear: both;

}

/* header */

#banner {
    position: fixed;
    left: 75px;
    background: white;
}

#title {
    color: $phill-grn;
    background: #ffef19;
    border-bottom: 2px solid $phill-grn;
    opacity: 0.9;

    width: 120%;
    position: relative;
    margin: 0 auto;
    padding-left: 15px;
}

.head {
    position: relative;
    background: white;
    z-index: 3;
}

.menu {
    margin-left: 50px;
    width: 110%;

    background: white;
    border-bottom: 2px solid $phill-grn;
}

.tiny-parallax {
    width: 100%;
}

.logo {
    opacity: 0.9;

    position: relative;
    width: 25%;
    padding: 25px;
}

#quote {
    position: relative;
    width: 20.6%;
    padding: 25px;
    margin-left: 25px;
    top: -27px;
    text-align: justify;

    background: $phill-blu;
    border: 3px solid $phill-grn;
    
    p {
	font-size: 14px;
	padding: 0px;
    }
}

.small-img {
    width: 50%;
    border-radius: 5px;
    margin: 10px;
}

.nav-pills {
  > li {
    float: left;

    // Links rendered as pills
    > a {
      border-radius: $nav-pills-border-radius;
    }
    + li {
      margin-left: 0px;
    }

    // Active state
    &.active > a {
      &,
      &:hover,
      &:focus {
        color: $nav-pills-active-link-hover-color;
        background-color: $nav-pills-active-link-hover-bg;
      }
    }
  }
}

/* Content */

.box {
/*    border: solid $phill-grn 2px;*/
}

.content-area {
    height: 80%;
    margin: 8.5em 0 0 20em;
}

#content-box {
    @extend .box;
    @extend .content-area;

    width: 65%;
    position: fixed;

}

#content-space {
    @extend.content-area;
}

.content {
    @extend .content-area;

    position: relative;
    width: 75%;
    overflow: hidden;
    margin-bottom: 3em;
}

#bottom-half {
    position: relative;
}

/* footer */

#footer {
    @extend #title;

    border-bottom: none;
    border-top: 2px solid $phill-grn;

    left: 90px;
    position: fixed;
    bottom: 0;
}

/* misc */

.alert {
    z-index: 3;
    margin: 1em 0 1em 1em;
    width: 50%;
    float: right;
}


