$(document).ready(function() {
    var url = $('.snitch').children('a').attr('href');
    //what happens with multiples?
    //$('.feature-image').wrap("<a href='" + url + "' target='_blank' class=''></a>");
});
