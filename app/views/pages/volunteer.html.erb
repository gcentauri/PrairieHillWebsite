<h2 class="section-title">
  Volunteer for Country Fair Activities
</h2>

<table class="table">
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shifts.each do |shift| %>
  <tr>
    <td><%= shift.title %></td>
    <td><%= shift.time %></td>
    <td><%= shift.vols_needed %></td>
  </tr>
<% end %>
</table>
