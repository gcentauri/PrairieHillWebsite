<% if user_signed_in? %>
  <em><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></em>
<% else %>
  <%= link_to '/users/auth/facebook' do %>
    <%= image_tag "fb-login.png", class: 'bottom-shadow' %>
  <% end %>
  <strong>
    <%= link_to 'Sign in with Email', new_user_session_path, class: 'button large expanded padded rounded-five bottom-shadow' %>
  </strong>
<% end %>
